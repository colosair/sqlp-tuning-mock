<!--meta
번호: 4
대상장: 2
대상절: 2.1
절제목: 실행계획 확인
문제유형: 적절한_것
보조자료: 없음
DBMS: 오라클
정답: 4
선택지유형: 서술형
함정유형: [경계_오해]
대상개념: [실행계획, DBMS_XPLAN, 예상_실행계획]
자극물밀도: 없음
계산요구: 없음
출처: 생성
상태: 검수완료
-->

## 문제 4

### 문제

`EXPLAIN PLAN`으로 적재한 예상 실행계획을 `DBMS_XPLAN.DISPLAY`로 조회했을 때, 출력된 계획 트리를 어떻게 읽어야 하는지 — 단계의 실행 순서, Predicate 섹션의 두 조건, format 수준 — 에 대한 설명으로 가장 적절한 것은?

### 선택지

① `DBMS_XPLAN.DISPLAY`가 출력한 계획에서 단계의 실행 순서는 표에 적힌 위에서 아래 순서 그대로이므로, 표에 먼저 나온 Operation이 항상 먼저 수행되고 들여쓰기 깊이는 실행 순서와 무관한 표시상의 장식일 뿐이다.

② 계획 하단 Predicate 섹션의 access 조건과 filter 조건은 이름만 다를 뿐 하는 일은 같아서, 어느 쪽에 적혀 있든 그 조건이 인덱스나 조인의 탐색 범위를 좁혀 준다는 뜻으로 읽으면 된다.

③ `DBMS_XPLAN.DISPLAY`의 `format` 인자를 `'BASIC'`으로 낮추면 Operation·Name에 더해 Predicate 섹션의 access·filter 조건까지 가장 상세히 함께 출력되므로, 각 단계가 어떤 조건으로 걸러지는지 한눈에 볼 수 있다.

④ 계획 트리에서 단계의 실행 순서는 적힌 차례가 아니라 들여쓰기로 드러나는 부모-자식 관계로 읽어야 하며, 가장 안쪽(깊이가 깊은) 자식부터 수행해 그 결과를 부모로 올린다. Predicate 섹션에서 access 조건은 인덱스·조인 진입 시 탐색 범위를 좁히는 조건이고 filter 조건은 그렇게 얻은 행을 추가로 걸러 내는 조건이라, 둘을 구분해 읽어야 스캔 범위 문제를 짚어낼 수 있다.

---

### 정답 — ④

### 왜 ④인가

계획을 '읽는 규칙' 세 가지 — 실행 순서, Predicate 두 조건의 역할, format 수준 — 를 각각 정확히 짚어야 합니다.

```text
실행 순서   : 위→아래(X). 들여쓰기 부모-자식(O)
             가장 깊은 자식부터 수행 → 결과를 부모로 올림
Predicate   : access 조건 = 진입 탐색 범위를 좁힘(시작·종료점)
             filter 조건 = 얻은 행을 추가로 걸러 냄(범위는 그대로)
format 수준 : BASIC   = Operation·Name만 (Predicate·Cost 생략)
             TYPICAL = 기본, Cost·카디널리티 포함
             ALL     = Predicate 섹션까지 상세
```

실행계획 트리에서 단계의 수행 순서는 적힌 차례가 아니라 **들여쓰기가 드러내는 부모-자식 관계**로 읽습니다. 가장 안쪽 자식이 먼저 수행되고 그 결과가 부모로 올라가는 식입니다. 또 Predicate 섹션의 **access 조건은 탐색 범위를 좁히는 조건**이고 **filter 조건은 얻은 행을 추가로 걸러 내는 조건**이라 역할이 다릅니다. ④는 이 두 규칙을 정확히 서술해 옳습니다.

나머지는 모두 계획을 읽는 규칙의 **경계를 뭉갠** 서술입니다. ①은 실행 순서를 "위에서 아래 그대로"라 하며 들여쓰기의 의미를 지웠지만, 순서는 부모-자식 관계로 정해집니다. ②는 access와 filter를 같은 역할로 뭉갰지만, filter는 탐색 범위를 좁히지 못하고 이미 얻은 행을 걸러 낼 뿐입니다. ③은 `'BASIC'`이 Predicate까지 가장 상세히 보여 준다고 했지만, `'BASIC'`은 오히려 Operation·Name만 남기고 Cost·카디널리티·Predicate 섹션을 생략하는 최소 수준이라 조건을 보려면 `'ALL'` 등으로 올려야 합니다.

### 오답 이유

| 선택지 | 판정 | 이유 |
|:-:|:-:|---|
| ① | ✗ | 단계 실행 순서는 적힌 순서가 아니라 들여쓰기 부모-자식 관계로 읽어, 가장 깊은 자식부터 수행됩니다. 들여쓰기를 장식으로 본 것은 읽기 규칙의 경계를 뭉갠 서술입니다 |
| ② | ✗ | access는 탐색 범위를 좁히는 조건, filter는 얻은 행을 걸러 내는 조건으로 역할이 다릅니다. 둘을 같은 일로 본 것은 경계 오해입니다 |
| ③ | ✗ | `'BASIC'`은 Operation·Name만 남기고 Cost·카디널리티·Predicate 섹션을 생략하는 최소 수준입니다. 조건을 상세히 보려면 `'ALL'` 등으로 올려야 합니다 |
| ④ | **○** | 실행 순서는 들여쓰기 부모-자식으로 읽어 깊은 자식부터 수행하며, access(범위 축소)와 filter(행 제거)를 구분해 읽어야 스캔 범위 문제를 짚을 수 있습니다 |

---

## ✅ 이 문제의 핵심

1. 계획 트리의 **실행 순서**는 적힌 차례가 아니라 **들여쓰기 부모-자식 관계**로, 가장 깊은 자식부터 수행됩니다.
2. Predicate 섹션의 **access 조건**은 탐색 범위를 좁히고, **filter 조건**은 얻은 행을 추가로 걸러 냅니다(범위는 그대로).
3. `format`을 `'BASIC'`으로 낮추면 Operation·Name만 남고 Cost·카디널리티·Predicate가 생략됩니다.
4. `EXPLAIN PLAN`+`DBMS_XPLAN.DISPLAY`로 보는 것은 실행 전 **예상(추정) 실행계획**입니다.

📌 한 줄 정리: 실행 순서는 들여쓰기 부모-자식으로 읽고 access·filter를 구분해 본다는 ④가 옳고, 위에서 아래 순서(①)·두 조건 동일시(②)·BASIC이 가장 상세(③)는 계획 읽기 규칙의 경계를 뭉갠 서술입니다.
