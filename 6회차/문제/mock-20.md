<!--meta
번호: 20
대상장: 7
대상절: 7.1
절제목: Lock
문제유형: 적절하지_않은_것
보조자료: 표
DBMS: 오라클
정답: 3
선택지유형: 서술형
함정유형: [정반대_진술]
대상개념: [Select_For_Update, Share_Lock, TX_Lock]
자극물밀도: Lock표
계산요구: 없음
출처: 생성
상태: 검수완료
-->

## 문제 20

### 문제

배차 관제 화면에서 세션 27이 특정 배차요청 한 건을 `SELECT … FROM 배차요청 WHERE 배차번호 = 7180 FOR UPDATE`로 잠근 뒤 아직 커밋하지 않았다. 그 사이 세션 38이 같은 배차번호(7180) 행을 다시 다루려 한다. 이때 `v$lock`을 조회한 결과가 아래와 같다. `SELECT … FOR UPDATE`가 건 행 잠금(TX 락)의 획득·대기·옵션 동작에 대한 해석으로 가장 적절하지 <u>않은</u> 것은?

#### [아 래]

```text
[ v$lock — 배차번호 7180 행에 걸린 TX(트랜잭션) 락 ]
SID   TYPE   ID1        LMODE   REQUEST   BLOCK
------------------------------------------------
 27   TX     393241        6        0        1
 38   TX     393241        0        6        0

* TX 모드 코드:  6 = X(Exclusive, 행 배타)
* SID 27 : SELECT … FOR UPDATE 로 해당 행에 X(6) 보유 (BLOCK=1)
* SID 38 : 같은 행을 잠그려 X(6)를 REQUEST 하며 대기
```

### 선택지

① 세션 27은 `SELECT … FOR UPDATE`로 배차번호 7180 행에 TX 락을 X(6) 모드로 획득했고, BLOCK=1로 같은 행을 원하는 다른 세션을 막고 있다. 이는 테이블 전체가 아니라 그 행에 대한 잠금이다.

② 세션 38이 같은 행을 기본 `SELECT … FOR UPDATE`(WAIT, 무한 대기)로 잠그려 하면 X(6)를 REQUEST한 채, 세션 27이 커밋하거나 롤백해 락을 놓을 때까지 대기한다.

③ 세션 38이 `SELECT … FOR UPDATE NOWAIT`를 쓰면 세션 27이 이미 잠근 행이라도 대기 없이 즉시 그 행의 락을 획득해 곧바로 갱신을 이어간다.

④ 세션 38이 `SELECT … FOR UPDATE SKIP LOCKED`를 쓰면 세션 27이 잠근 7180 행은 건너뛰고, 잠기지 않은 다른 배차요청 행만 잠가 대기 없이 처리 대상을 확보한다.

---

### 정답 — ③

### 왜 ③인가

`SELECT … FOR UPDATE`는 조회한 행에 **TX 락을 배타(X, 6) 모드**로 잡습니다. 표에서 세션 27이 `LMODE=6, BLOCK=1`로 7180 행을 배타 점유하고, 세션 38이 같은 행에 `REQUEST=6`으로 대기하는 그림이 바로 그 충돌입니다. 같은 행을 배타로 원하는 두 세션은 공존할 수 없으므로, 뒤에 온 세션은 앞 세션이 커밋/롤백으로 락을 놓을 때까지 기다립니다(②).

③은 `NOWAIT`의 동작을 **정반대로** 뒤집었습니다. `FOR UPDATE NOWAIT`는 대상 행이 이미 잠겨 있으면 **대기하지 않고 즉시 오류(ORA-00054, resource busy)를 던지며 락을 얻지 못합니다**. "대기 없이 즉시 획득해 갱신을 이어간다"가 아니라 "대기 없이 즉시 실패한다"가 옳습니다. 표의 세션 27이 7180 행을 X(6)로 점유 중이므로, 세션 38이 `NOWAIT`로 시도하면 REQUEST 상태로 매달리지도 못하고 곧바로 오류로 튕깁니다. 그래서 표가 보여 주는 잠금 상태와 정면으로 어긋나는 진술이 ③이고, 부적절한 것은 ③입니다.

①②④는 각각 FOR UPDATE가 건 행 단위 TX 배타 락(①), 기본 WAIT의 무한 대기(②), `SKIP LOCKED`가 잠긴 행을 건너뛰고 나머지만 잠그는 동작(④)을 옳게 설명합니다.

### 오답 이유

| 선택지 | 판정 | 이유 |
|:-:|:-:|---|
| ① | ○ | 세션 27은 FOR UPDATE로 7180 행에 TX X(6)를 잡고 BLOCK=1로 상대를 막습니다. 테이블 전체가 아니라 그 행에 대한 잠금입니다 |
| ② | ○ | 기본 FOR UPDATE(WAIT)는 X(6)를 REQUEST한 채, 세션 27이 커밋·롤백으로 락을 놓을 때까지 대기합니다. 표의 두 번째 행 그대로입니다 |
| ③ | **✗** | `FOR UPDATE NOWAIT`는 이미 잠긴 행이면 즉시 ORA-00054로 실패하지 락을 획득하지 않습니다. "대기 없이 즉시 획득"은 방향이 반대입니다 |
| ④ | ○ | `SKIP LOCKED`는 잠긴 7180 행을 건너뛰고 잠기지 않은 다른 행만 잠가 대기 없이 처리 대상을 확보합니다 |

---

## ✅ 이 문제의 핵심

1. **`SELECT … FOR UPDATE`는 조회 행에 TX 락을 X(6) 모드로 잡습니다.** 테이블 전체가 아니라 행 단위 잠금입니다.
2. **기본(WAIT)은 상대가 커밋·롤백할 때까지 대기**합니다. 표의 REQUEST=6·BLOCK=1이 그 충돌을 보여 줍니다.
3. **`NOWAIT`는 이미 잠긴 행이면 즉시 ORA-00054로 실패**하고 락을 얻지 못합니다 — 즉시 획득이 아닙니다.
4. **`SKIP LOCKED`는 잠긴 행을 건너뛰고 나머지만 잠가** 대기 없이 처리 대상을 확보합니다(큐 소비 패턴).

📌 한 줄 정리: `SELECT … FOR UPDATE`가 건 행 단위 TX X(6) 락은 같은 행을 원하는 세션을 대기시키며, `NOWAIT`는 이미 잠긴 행에서 즉시 실패하므로 "대기 없이 즉시 획득한다"는 ③은 옵션 동작을 정반대로 뒤집은 진술입니다.
