# SQLP 과목 III 튜닝 스타일 모의문제 — 생성 지침

> **SQL 자격검정 실전문제(일명 노랭이) 과목 III의 출제 *스타일*을 재현한 원본 모의문제**입니다. 상용 교재의 문항을 전사·복제한 것이 **아니라**, 그 문제집 121문항을 집계해 얻은 **통계(분포·함정 패턴)와 난이도 특성**만을 참조해 **새로 창작**한 것입니다. 저작권 제약이 없어 공개(public) 공유가 가능합니다.

## 성격과 원칙

- **원본 창작(clean-room).** 노랭이의 구체적 지문·시나리오·수치·테이블명을 재사용하지 않습니다. 재현하는 것은 *형식과 난이도*이지 *문항*이 아닙니다.
- **자족적.** 비공개 노랭이 전사 저장소나 교재 노트에 링크하지 않습니다. 해설은 각 파일 안에서 완결됩니다.
- **한국어 존댓말**, 실제 오라클·SQL Server 동작에 충실.

---

## ★ 품질 스펙 — 재현 목표 (이 문서의 핵심)

실제 노랭이 121문항에서 실측한 수준. 새 문항은 이 6축을 **충족해야** 하고 verify가 강제합니다.

### 1. 실행계획은 구식 포맷, 밀도는 노드 수로
노랭이 실행계획에는 **Predicate Information(access/filter) 0건, A-Rows·E-Rows·Buffers·Starts 실측열 0건**입니다. 두 포맷만 씁니다.
- **구식 들여쓰기 트리**: `id  parent-id  Operation (COST=n CARD=n BYTES=n)`
- **`Id | Operation | Name` 표** (+ 필요 시 `Pstart | Pstop | TQ | IN-OUT`), `*` 마커는 붙이되 **하단 Predicate 절은 생략**
밀도는 **병렬/파티션 플랜 10행 이상**(PX SEND/RECEIVE QC, TQ1x, PCWP/PCWC, PX BLOCK ITERATOR)으로 냅니다.
> **금지**: `A-Rows`, `E-Rows`, `Buffers`, `Starts`, `Predicate Information`, `access(`, `filter(`. 이건 노랭이가 안 쓰는 현대 포맷이라 넣으면 시대착오입니다. verify가 차단합니다.

### 2. 정통 TKPROF + 산수 강제
Call 7열 표 `Count | CPU | Elapsed | Disk | Query | Current | Rows` + Row Source Operation(`cr= pr= pw= time= us`). 해설은 **산수 → 비율 판정 → 병목 지목** 3단으로. 대표 공식:
- 클러스터링 팩터 ≈ 테이블 블록 I/O(cr) ÷ ROWID 수
- BCHR = ((query + current) − disk) / (query + current) × 100
- ArraySize = 총 rows ÷ (fetch 횟수 − 1)  ← 마지막 no-more-data fetch의 −1
세트에 **최소 3~4문항**.

### 3. 세션/Lock은 정식 표
TX1/TX2 × t1~t4 타임라인 그리드, `DBA_LOCK` 6열 모니터링 결과표(LOCK_TYPE·MODE_HELD·MODE_REQUESTED), 필요 시 저장 프로시저 전문. 오라클 vs SQL Server 최종값 산출을 요구.

### 4. 다분할 파티션 DDL
`partition by range(...) ( partition p1 values less than(...), ... )` 다분할, range+hash 서브파티션, PQ_DISTRIBUTE 힌트 + 병렬 플랜 판독.

### 5. 다층 수치 발문 + `(단, …)` 차단 조건
시나리오 문항은 **데이터량 2개 이상**(예: 고객 100만, 월 주문 2000만) + 증상 + `(단, Isolation Level을 조정하지 않았고 …)` 같은 오답경로 차단 단서. 부정형은 `가장 적절하지 <u>않은</u> 것은?`로 `않은`에 밑줄.

### 6. 오답은 자극물의 숫자·노드에 결부
오답을 박스의 **실제 수치·플랜 노드에 묶어** 계산 없이는 소거 불가하게. **`항상·전혀·반드시·언제나·모두·절대` 같은 절대수식어 tell을 쓰지 않습니다**(문항당 최대 1회). 이 tell이 있으면 계산 없이 소거되어 난이도가 무너집니다.

---

## 함정 카탈로그 (오답 제조 — `함정유형` 필드가 참조)

| 함정유형 | 방법 |
|---|---|
| `정반대_진술` | 옳은 문장의 핵심어를 반대로 뒤집기 (좋다↔나쁘다, 생성↔생략) |
| `같은것_다른이름` | 동의어 둘 병치로 `A + A` 중복 유도 (Service Time + CPU Time) |
| `하위항목_전체화` | 부분집합을 전체 등식에 끼우기 (논리적 I/O = query만? → query+current) |
| `범위밖_개념` | 정의역 밖 개념을 그럴듯하게 (Idle Time을 Response Time에) |
| `경계_오해` | 옵션 단독 동작 경계를 뭉개기 (traceonly 단독은 수행함) |
| `원인_오귀속` | 수치는 맞되 병목 원인을 엉뚱한 요인에 연결 |
| `오라클↔SQLServer_뒤바꿈` | 두 DBMS 동작을 서로 바꾸거나 한쪽으로 통일 |
| `정규화_가정_오류` | 대소문자·주석·공백이 정규화돼 공유된다고 착각 유도 |
| `별개_축_혼동` | 독립인 두 성질을 인과로 연결 (파싱 비용 ≠ 계획 품질) |
| `반사적_연상` | 키워드-정답 반사 연결을 함정으로 (고르게 분산=Hash?) |
| `핵심정리목록밖` | 선택지 3개를 표준 목록과 대응시키고 하나만 목록 밖 |
| `부분진실_형변환` | 조건 하나(묵시적 형변환·키 가공)로 최선→최악 뒤집기 |
| `공통단서_무력화` | 모두에 해당하는 단서를 판별 근거로 착각시키기 |
| `값스왑` | 두 결과값을 서로 바꾸거나 2×2 조합을 전부 채워 소거 방해 |

---

## 문항 md 스키마

```yaml
<!--meta
번호: 1
대상장: 3            # 스타일 프로파일의 장 (분포 확인용)
대상절: 3.2
절제목: 테이블 액세스 최소화
문제유형: 적절한_것    # 적절한_것 | 적절하지_않은_것 | 직접지목형
보조자료: 트레이스     # 없음|텍스트|SQL|실행계획|트레이스|표|그림|복합
DBMS: 오라클          # 오라클 | 공통 | SQL_Server
정답: 3
선택지유형: 서술형     # 서술형 | 코드형 | 혼합
함정유형: [원인_오귀속, 정반대_진술]      # 위 카탈로그의 값
대상개념: [클러스터링_팩터, 테이블_Random_액세스]   # 노랭이 개념태그 사전 어휘
자극물밀도: TKPROF_7열+RowSource          # 없음 | TKPROF_7열[+RowSource] | 실행계획_N행 | Lock표 | TXxt표 | 다분할DDL | SQL | 표
계산요구: 있음         # 있음 | 없음
출처: 생성            # 항상 '생성' (clean-room 표식)
상태: 검수완료         # 초안 | 검증대기 | 검수완료
-->
```

### 본문 구조 (순서 고정, 노랭이 121과 동일 깊이)
```
## 문제 N

### 문제
<다층 수치 발문. 필요 시 말미에 (단, …) 차단 조건>

#### [아 래]                      ← 보조자료 있을 때만 (####는 이 예외만 허용)
```text  또는  ```sql
<자극물: 노랭이 구식 포맷. 실측열/Predicate 금지>
```

### 선택지
① … ② … ③ … ④ …

---

### 정답 — N

### 왜 N인가
<산수 → 비율 판정 → 병목 지목. 계산요구 있음이면 ```text 계산 블록 필수>

### 오답 이유
| 선택지 | 판정 | 이유 |
|:-:|:-:|---|
| ① | ○/✗ | <자극물 숫자·노드에 결부> |
（4행 필수. 판정 = 진술의 참(○)/거짓(✗). 정답 행만 **볼드**.
  적절한_것·직접지목형 → 정답행 **○**, 나머지 ✗.  적절하지_않은_것 → 정답행 **✗**, 나머지 ○.）

## ✅ 이 문제의 핵심
1. …  (3~5개)

📌 한 줄 정리: <한 문장>
```

- **원문 해설·🔗 교재 링크·📄 책 푸터는 없습니다** — 출처가 책이 아니므로. `### 왜 N인가`가 자족 해설입니다.
- SQL은 ` ```sql `, 실행계획·트레이스·표는 ` ```text `.

## 저장 규칙 — 회차별 디렉토리
- 문항은 **회차(`N회차/`)** 단위로 나눕니다. 공통 파일(이 AGENTS.md·README·LICENSE·`_tools/`)은 최상위, 각 회차 세트는 `N회차/문제/`·`N회차/커버리지.md`·`N회차/모의고사_N회차.pdf`.
- 파일: `N회차/문제/mock-NN.md` (2자리 제로패딩, `번호`와 일치). 회차마다 `번호`는 1부터 다시 시작합니다.
- `번호` 1~20 연속·중복 없음. `대상개념` ⊆ 노랭이 `개념태그_사전.md`. `함정유형` ⊆ 위 카탈로그.
- **★ 정답 위치 스크램블(필수)**: 한 회차의 정답 번호는 ①②③④ 각 5개로 균등하게 두되, **문항 위치로 정답을 추정할 수 있는 규칙적 배열을 금지**합니다. `1,2,3,4,1,2,3,4…`(순환 오름)나 그 회전(`4,1,2,3…`), 등차·긴 동일 연속을 쓰지 말고 **무작위처럼 흩뿌립니다**(순환 오름/내림 3연속 이하, 동일값 2연속 이하 권장). verify가 순환 패턴을 가드합니다. 선택지 순서는 정답이 특정 위치에 몰리지 않게 자연스럽게 섞습니다(실제 시험의 보기 셔플과 동일).
- 검증: `python _tools/verify.py <회차>` (자기정합·오답표·태그·분포 + 시대착오/밀도 가드). 렌더: `python _tools/render.py <회차>`. 회차 생략 시 `1회차`.
- **실행계획/표 정렬**: 렌더러가 파이프 표·구식 괄호 트리를 **디스플레이 폭(한글=2배폭) 기준으로 자동 재정렬**합니다. 소스는 글자 수로만 맞춰도 되지만, 파이프 표는 `|` 구분·구식 트리는 `( Cost Card Bytes )` 헤더 형태를 유지해야 자동 정렬이 걸립니다.
