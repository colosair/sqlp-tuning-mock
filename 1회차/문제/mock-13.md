<!--meta
번호: 13
대상장: 5
대상절: 5.2
절제목: 소프트 파싱과 커서 공유
문제유형: 적절한_것
보조자료: SQL
DBMS: 오라클
정답: 4
선택지유형: 서술형
함정유형: [정규화_가정_오류]
대상개념: [바인드_변수, 커서_공유]
자극물밀도: SQL
계산요구: 없음
출처: 생성
상태: 검수완료
-->

## 문제 13

### 문제

세 세션이 논리적으로 같은 결과를 얻으려고 아래 SQL을 각각 수행했다. 세 SQL은 **대소문자·공백·주석**만 다르다. 수행 뒤 라이브러리 캐시를 요약한 결과가 함께 주어졌다. 커서 공유(Cursor Sharing)에 대한 설명으로 가장 적절한 것은?

#### [아 래]

```sql
-- 세션 A
select * from orders where cust_id = 100;

-- 세션 B  (SELECT 를 대문자로)
SELECT * from orders where cust_id = 100;

-- 세션 C  (from 앞 공백 2칸 + 끝에 주석)
select *  from orders where cust_id = 100;  -- daily batch
```

```text
[ 라이브러리 캐시 요약 ]
SQL_ID           SQL_TEXT (선두 요약)                          PARSE_CALLS  EXECUTIONS
-------------------------------------------------------------------------------------
9k2m7b3d0x1aq    select * from orders where cust_id = 100            1          1
7f3p5c8e2y6bh    SELECT * from orders where cust_id = 100            1          1
2d5x1a4f9z0ck    select *  from orders where cust_id = 100 --...     1          1
```

### 선택지

① 세션 C 끝의 `-- daily batch`는 주석이라 옵티마이저가 무시하므로, `2d5x1a4f9z0ck`의 PARSE_CALLS 1은 세션 A의 커서를 그대로 재사용한 소프트파싱 기록이다.
② 오라클은 파싱 전 텍스트를 대문자로 바꾸고 여백·주석을 지워 정규화하므로, 세션 B의 `SELECT`와 세션 C의 공백 2칸은 흡수되고 세 SQL은 한 커서를 공유한다.
③ 세 SQL이 `cust_id = 100`이라는 같은 상수를 쓰므로 바인드 변수 없이도 커서가 공유되며, 표의 SQL_ID 3개는 EXECUTIONS가 쌓이면 하나로 합쳐질 임시 항목이다.
④ 세 SQL은 대소문자·공백·주석만 다르고 의미는 같지만, 텍스트가 문자 단위로 일치하지 않아 서로 다른 SQL_ID로 각각 하드파싱되었다.

---

### 정답 — ④

### 왜 ④인가

라이브러리 캐시 요약이 답을 그대로 보여 줍니다. 논리적으로 같은 세 SQL인데 **SQL_ID가 세 개(9k2m…, 7f3p…, 2d5x…)** 로 갈렸고, 각각 `PARSE_CALLS 1 / EXECUTIONS 1`입니다.

```text
9k2m...  ← 세션 A : 원본
7f3p...  ← 세션 B : SELECT 만 대문자
2d5x...  ← 세션 C : from 앞 공백 2칸 + 끝 주석
→ 텍스트가 다르면 SQL_ID가 다르고, 커서를 공유하지 못해 각각 하드파싱
```

오라클의 기본(EXACT) 커서 공유는 **SQL 텍스트를 문자 단위로 비교**합니다. 대소문자·공백·주석 하나만 달라도 다른 텍스트로 간주되어 라이브러리 캐시에서 기존 커서를 찾지 못하고, 새 커서를 만들며 **하드파싱**합니다. 요약표의 세 SQL_ID가 바로 그 결과입니다. ④이 정확합니다.

②는 "대문자화·여백/주석 제거로 텍스트를 정규화해 공유한다"는 **정규화 가정의 오류**입니다. 세션 B의 `SELECT`와 세션 C의 공백 2칸이 실제로 흡수되었다면 요약표에는 SQL_ID가 하나만 남고 `EXECUTIONS 3`이어야 합니다. 표는 SQL_ID 3개에 각각 `EXECUTIONS 1`이므로 그런 정규화가 없었음을 반증합니다.

③은 오히려 반대입니다. **리터럴(`cust_id = 100`)을 그대로 쓰면** 값이 바뀔 때마다 텍스트가 달라져 커서 공유가 깨집니다. 또한 SQL_ID는 텍스트 해시라 나중에 병합되는 "임시 항목"이 아닙니다 — 각 행의 `PARSE_CALLS 1`은 각자 하드파싱했다는 확정 기록입니다. 공유하려면 상수를 **바인드 변수(:b1)** 로 바꿔야 합니다.

①의 주석은 실행계획 결과에는 영향이 없더라도 **SQL 텍스트의 일부**입니다. 세션 C의 `-- daily batch`가 붙은 순간 텍스트가 달라져 별도 SQL_ID(`2d5x1a4f9z0ck`)가 생겼고, 그 행의 `PARSE_CALLS 1`은 세션 A 커서의 재사용(소프트파싱)이 아니라 **자기 커서를 새로 만든 하드파싱**입니다.

### 오답 이유

| 선택지 | 판정 | 이유 |
|:-:|:-:|---|
| ① | ✗ | 주석도 SQL 텍스트의 일부입니다. `-- daily batch` 때문에 세션 C는 별도 SQL_ID(2d5x…)를 얻었고, 그 행의 PARSE_CALLS 1은 재사용이 아니라 새 커서를 만든 하드파싱입니다 |
| ② | ✗ | 기본 커서 공유는 대문자화·여백/주석 제거로 정규화하지 않습니다. 세션 B의 SELECT와 세션 C의 공백 2칸이 흡수됐다면 SQL_ID 하나에 EXECUTIONS 3이어야 하는데, 표는 셋에 각각 1입니다 |
| ③ | ✗ | 같은 상수라고 자동 공유되지 않으며, SQL_ID는 텍스트 해시라 EXECUTIONS가 쌓여도 병합되지 않습니다. 리터럴을 바인드 변수로 바꿔 텍스트를 같게 만들어야 공유됩니다 |
| ④ | **○** | 세 SQL은 의미가 같아도 텍스트가 문자 단위로 다릅니다. 요약표의 SQL_ID가 셋(9k2m·7f3p·2d5x)으로 갈려 각각 하드파싱되었음을 보여 줍니다 |

---

## ✅ 이 문제의 핵심

1. **기본 커서 공유는 텍스트를 문자 단위로 비교**합니다. 대소문자·공백·주석이 하나만 달라도 다른 SQL_ID입니다.
2. **텍스트가 다르면 = 하드파싱.** 요약표의 SQL_ID 개수가 곧 하드파싱한 커서 개수입니다.
3. **리터럴 대신 바인드 변수.** 값이 바뀌어도 텍스트가 같아져 커서를 공유(소프트파싱)할 수 있습니다.
4. 옵티마이저가 텍스트를 정규화해 공유해 준다는 것은 흔한 착각입니다 — 기본 모드에서는 그런 정규화가 없습니다.

📌 한 줄 정리: 오라클 기본 커서 공유는 SQL 텍스트를 문자 단위로 비교하므로 대소문자·공백·주석만 달라도 각각 하드파싱되며, 공유하려면 리터럴을 바인드 변수로 바꿔 텍스트를 동일하게 만들어야 합니다.
